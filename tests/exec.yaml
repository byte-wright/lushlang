order: 3
name: Exec
tests:
  - name: execArrays
    description: 
    code: |
      files, stderr, err = execVar("ls", "-1", "./exec/files")
      println(join(files, ", "))
    expected: |
      bar.txt, foo.txt
